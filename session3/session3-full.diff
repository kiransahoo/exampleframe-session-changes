diff --git a/ONBOARDING.md b/ONBOARDING.md
index c44fa94..47f3ce5 100644
--- a/ONBOARDING.md
+++ b/ONBOARDING.md
@@ -10,6 +10,43 @@ You do (1) once per app, and (2) once per service you want covered.
 
 ---
 
+## Which URL to use - your domain's cell, not the meta
+
+Every domain runs its own #ExampleFrame# cell with its own URL, plus there is one meta
+orchestrator that fronts all of them:
+
+| URL | What it is | Use it for |
+|---|---|---|
+| `https://order.tb.com` | The **order** domain's cell | Anything about order-domain services |
+| `https://payment.tb.com` | The **payment** domain's cell | Anything about payment-domain services |
+| `https://claims.tb.com` | The **claims** domain's cell | Anything about claims-domain services |
+| `https://meta.tb.com` | The **meta** orchestrator | Cross-domain questions, or when you don't know which domain owns a service |
+
+**Default to your own domain's URL.** The meta forwards an in-domain question to your cell
+verbatim and your cell's own router runs it, so the answer is the same either way - but going
+direct is strictly better for a team that knows its services:
+
+- **Richer prompting.** Your cell can prompt you interactively for infrastructure scope it
+  can't infer (namespace, schema, tables) and then run the playbook. The meta only collects
+  user-level params (time range, service) - for anything unmapped it falls back to a
+  free-form investigation instead of a playbook run.
+- **One less hop, one less LLM call.** Every question through the meta pays a routing hop
+  plus a parameter-extraction LLM call before your cell even starts.
+- **Live agent panels.** The cell streams each agent's raw findings as they land. Through
+  the meta you get the cell's finished answer as a single block.
+- **Access scoping.** Cell URLs line up with per-domain access groups - your team's
+  credentials work on your cell without granting anything cross-domain.
+
+**When the meta is the right door:** an incident that spans domains ("checkout is slow AND
+claims batch is backed up"), a service you can't place, or on-call/SRE work across domains.
+Meta routing works off the `domain` field the owning cell stamps on each service mapping -
+which is one more reason mappings are registered on the cell, never on the meta (see Part 2).
+
+Approvals for write operations (restart, scale, delete) work identically on both doors: the
+same authorization panel appears wherever you asked, and nothing executes until you approve.
+
+---
+
 ## Part 1 - #ExampleFrame# 1.0 (instrumentation)
 
 #ExampleFrame# 1.0 is a ByteBuddy-based Java agent (`-javaagent:#exampleframe#.jar`) that auto-instruments your JVM applications at the **application package level** and ships OpenTelemetry traces, metrics, and logs to Azure Application Insights with **zero code changes**.
@@ -33,6 +70,15 @@ With a service mapping in place, that same question routes **deterministically**
 
 ### What a service mapping looks like
 
+**The mapping form is generated from your cell's agents.** Each domain's Settings modal
+asks only for the fields its own agents can use - the form is built from
+`GET /api/settings/service-context/schema`, which reflects the cell's configured agent
+list. An order cell running Kubernetes + Oracle shows K8s and Oracle fields; a payment
+cell running Kubernetes + PostgreSQL shows K8s and Postgres fields instead. The meta shows
+no infrastructure fields at all - it only routes by domain. So don't expect the exact
+table below on every cell; it's the field set for a cell running K8s + Oracle + Azure
+Monitor + dbt.
+
 Each mapping ties together everything #ExampleFrame# needs to know about one service:
 
 | Field | What it is | Example |
@@ -53,7 +99,9 @@ Each mapping ties together everything #ExampleFrame# needs to know about one service
 
 ### How to add a service mapping (UI)
 
-1. Open the #ExampleFrame# 2.0 terminal: `https://<your-#exampleframe#-host>/`.
+1. Open **your domain's** #ExampleFrame# terminal (e.g. `https://order.tb.com/`). Register
+   mappings on the cell that owns the service, never on the meta - the cell stamps its
+   `domain` on the mapping, and that stamp is what the meta uses to route questions to you.
 2. Click **`[SETTINGS]`** in the header.
 3. Scroll to **"Service Context Mapping"** at the top of the modal.
 4. Fill in the fields above and click **`SAVE MAPPING`**.
@@ -150,7 +198,7 @@ For that to work, #ExampleFrame# needs to know **which Java package lives in which r
 
 ### How to add a VCS mapping (UI)
 
-1. Open #ExampleFrame#: `https://<your-#exampleframe#-host>/`.
+1. Open **your domain's** #ExampleFrame# terminal (e.g. `https://order.tb.com/`).
 2. Click **`[SETTINGS]`** in the header.
 3. Scroll past *"Service Context Mapping"* and *"Current Service Mappings"* to **"VCS Package Mappings"**.
 4. In the **Add Mapping** form:
@@ -212,6 +260,7 @@ Set both for every service you care about. One without the other only gets you h
 |---|---|---|
 | Orchestrator says *"no agents available"* intermittently | Nacos heartbeat or child agent restarted briefly | Check `kubectl get pods -n #exampleframe#` for restarts; loosen Nacos heartbeat windows |
 | K8s agent queries the wrong namespace | Mapping missing, or alias doesn't match the user's wording | Add the user's exact wording to the **Aliases** field |
+| Meta asks *"which domain owns this service?"* for a service you mapped | Mapping was registered on the meta (no `domain` stamp) or on the wrong cell | Re-register on the **owning** cell's URL; the cell stamps `domain`, which the meta routes on |
 | Mapping saved in UI doesn't take effect | Browser cache | Hard-refresh (Cmd-Shift-R), then re-prompt |
 | K8s agent says "Deployment not found" | `workload` field has the pod name (dynamic) instead of the deployment name | Replace with the Deployment name; verify with `kubectl get deployment <name> -n <ns>` |
 | Prod responses are terser than non-prod | Different LLM (gpt-4o vs Claude) and/or `temperature` default of 1.0 | Match `temperature: 0.1` and use gpt-5 in prod |
diff --git a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/api/controller/A2aIngressController.java b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/api/controller/A2aIngressController.java
index f584ed8..66dff50 100644
--- a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/api/controller/A2aIngressController.java
+++ b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/api/controller/A2aIngressController.java
@@ -95,6 +95,7 @@ public class A2aIngressController {
     private final @Nullable PlaybookExecutor playbookExecutor;
     private final com.alibaba.cloud.ai.graph.checkpoint.@Nullable BaseCheckpointSaver checkpointSaver;
     private final @Nullable ThreadStore threadStore;
+    private final com.#exampleframe#.orchestrator.observation.cost.@Nullable SessionCostAccumulator sessionCostAccumulator;
 
     /**
      * Derived threadIds with a request currently executing. A caller-supplied contextId
@@ -117,7 +118,9 @@ public class A2aIngressController {
                                 @Autowired(required = false) @Nullable PlaybookExecutor playbookExecutor,
                                 @Autowired(required = false)
                                 com.alibaba.cloud.ai.graph.checkpoint.@Nullable BaseCheckpointSaver checkpointSaver,
-                                @Autowired(required = false) @Nullable ThreadStore threadStore) {
+                                @Autowired(required = false) @Nullable ThreadStore threadStore,
+                                @Autowired(required = false)
+                                com.#exampleframe#.orchestrator.observation.cost.@Nullable SessionCostAccumulator sessionCostAccumulator) {
         this.runner = runner;
         this.properties = properties;
         this.hitlAwareA2aClient = hitlAwareA2aClient;
@@ -127,6 +130,7 @@ public class A2aIngressController {
         this.playbookExecutor = playbookExecutor;
         this.checkpointSaver = checkpointSaver;
         this.threadStore = threadStore;
+        this.sessionCostAccumulator = sessionCostAccumulator;
     }
 
     /** Thread-history identity for delegated conversations - segregated from real users' thread lists. */
@@ -163,6 +167,11 @@ public class A2aIngressController {
                     "An investigation for this conversation is already running on this cell (threadId "
                             + threadId + "). Retry after it completes."));
         }
+        // NOTE deliberately NOT binding SessionCostAccumulator's ThreadLocal here: the ReAct
+        // path's graph model calls run on this servlet thread and are recorded by
+        // CostTrackingInterceptor - a request-wide binding would make the ObservationFilter
+        // record them a SECOND time. The playbook path binds inside PlaybookGateway/
+        // PlaybookExecutor, scoped to exactly the calls only the filter can see.
         try {
             // First-turn gate: prior state for a stable thread means this is a follow-up
             // turn - it continues in ReAct with restored context, never a fresh playbook.
@@ -182,11 +191,12 @@ public class A2aIngressController {
             String playbookAnswer = tryPlaybook(rawTask != null ? rawTask : input,
                     taskParams, threadId, followUpTurn);
             if (playbookAnswer != null) {
-                return ResponseEntity.ok(success(requestId, threadId, playbookAnswer));
+                return ResponseEntity.ok(success(requestId, threadId, playbookAnswer,
+                        threadId, contextId == null));
             }
 
             String output = runner.investigate(new InvestigationRunner.InvestigationTask(input, threadId, null));
-            return ResponseEntity.ok(success(requestId, threadId, output));
+            return ResponseEntity.ok(success(requestId, threadId, output, threadId, contextId == null));
         } catch (Exception e) {
             // A child agent in this cell needs approval. On the ReAct path the exception
             // arrives wrapped (ChildAgentHitlRuntimeException, possibly nested inside the
@@ -201,6 +211,7 @@ public class A2aIngressController {
                     sanitizeForLog(String.valueOf(e.getMessage())), e);
             return ResponseEntity.ok(error(requestId, -32000, e.getMessage() != null ? e.getMessage() : "Unknown error"));
         } finally {
+            com.#exampleframe#.orchestrator.observation.cost.SessionCostAccumulator.clearCurrentThreadId();
             inFlight.remove(threadId);
         }
     }
@@ -223,22 +234,36 @@ public class A2aIngressController {
         if (forwardedPlaybookId != null && forwardedPlaybookId.isBlank()) {
             forwardedPlaybookId = null;
         }
+        // A deliberate deterministic forward - the meta's playbook door pins an id for an
+        // explicit user reference, or sends the bare marker for its scored matches (the cell
+        // routes the text itself then). Either way the first-turn gate must not demote the
+        // turn to ReAct just because this conversation touched the cell before: the CALLER
+        // already judged this message to be a fresh playbook-shaped request, exactly the
+        // per-message judgement the direct door's gateway applies to its own traffic.
+        boolean deterministicForward = forwardedPlaybookId != null
+                || "true".equalsIgnoreCase(taskParams.get(
+                        com.#exampleframe#.orchestrator.federation.MetaPlaybookRouting.DETERMINISTIC_FORWARD_PARAM));
         if (followUpTurn) {
-            // A caller that names a playbookId made a deliberate deterministic forward (the
-            // meta's playbook door does) - the first-turn gate must not demote it to ReAct
-            // just because this conversation touched the cell before.
-            if (forwardedPlaybookId == null) {
+            if (!deterministicForward) {
                 logger.info("A2A thread {} is a follow-up turn - continuing in ReAct with restored context", threadId);
                 return null;
             }
-            logger.info("A2A thread {} is a follow-up turn but the caller forwarded explicit playbookId '{}' - honoring it",
-                    threadId, sanitizeForLog(forwardedPlaybookId));
+            logger.info("A2A thread {} is a follow-up turn but the caller made a deterministic forward ({}) - honoring it",
+                    threadId, forwardedPlaybookId != null
+                            ? "playbookId '" + sanitizeForLog(forwardedPlaybookId) + "'" : "cell routes the text");
         }
         // A forwarded id SELECTS the playbook - text matching must not be able to pick a
         // different one (or miss on re-phrased text) after the caller already resolved it.
-        PlaybookResolver.Resolution resolution = forwardedPlaybookId != null
-                ? playbookResolver.resolveById(forwardedPlaybookId, matchText, taskParams).orElse(null)
-                : playbookResolver.resolve(matchText, taskParams).orElse(null);
+        // The resolve runs LLM calls (matcher, param extraction) that only the observation
+        // filter can record - bound to the conversation thread so their spend lands in this
+        // turn's costData instead of nowhere (or, worse, a stale binding another request
+        // leaked on this pooled thread). Never seen by the graph interceptor: no double count.
+        final String pinnedId = forwardedPlaybookId;
+        PlaybookResolver.Resolution resolution =
+                com.#exampleframe#.orchestrator.observation.cost.SessionCostAccumulator.withThreadId(threadId,
+                        () -> pinnedId != null
+                                ? playbookResolver.resolveById(pinnedId, matchText, taskParams).orElse(null)
+                                : playbookResolver.resolve(matchText, taskParams).orElse(null));
         if (resolution == null) {
             if (forwardedPlaybookId != null) {
                 logger.warn("Caller-forwarded playbookId '{}' is unknown or not runnable on this cell - deferring to ReAct",
@@ -479,7 +504,10 @@ public class A2aIngressController {
                 hitlAwareA2aClient.clearOrchestratorThreadId();
             }
             investigationStateService.resolveClaimedHitl(taskId, claimed, output);
-            return ResponseEntity.ok(success(requestId, cellThread != null ? cellThread : taskId, output));
+            // Cost travels only when the real conversation thread is known - a HITL task id
+            // is not a session key and must not be looked up as one.
+            return ResponseEntity.ok(success(requestId, cellThread != null ? cellThread : taskId, output,
+                    cellThread, false));
         } catch (ChildAgentHitlException chained) {
             // The answered tool ran; the child raised the NEXT gate. Replace the consumed
             // record with the new pending task, preserving the original owner and thread.
@@ -589,7 +617,17 @@ public class A2aIngressController {
         return id != null ? id.toString() : "a2a-" + System.currentTimeMillis();
     }
 
-    private static Map<String, Object> success(String requestId, String taskId, String output) {
+    /**
+     * @param costThreadId the cost-tracked conversation thread whose accumulated snapshot this
+     *        response should carry, or null to attach no cost (an unknown thread must never be
+     *        looked up - a task id is not a session key)
+     * @param isolatedSnapshot true when the thread lived only for this request (no caller
+     *        contextId), so the snapshot is per-task and the caller adds it directly; false for
+     *        a stable conversation thread, whose CUMULATIVE snapshot the caller de-dupes with
+     *        its per-child watermark
+     */
+    private Map<String, Object> success(String requestId, String taskId, String output,
+                                        @Nullable String costThreadId, boolean isolatedSnapshot) {
         Map<String, Object> textPart = new HashMap<>();
         textPart.put("type", "text");
         textPart.put("text", output != null ? output : "");
@@ -606,6 +644,26 @@ public class A2aIngressController {
         result.put("status", "completed");
         result.put("output", output != null ? output : "");
 
+        // What this delegated turn cost - the cell's own LLM calls plus its child agents',
+        // accumulated under the cell-side conversation thread. Without this the caller (a
+        // meta) shows only its OWN llm spend for the conversation: the investigation it paid
+        // a cell for never reaches its cost panel. Plain agents send the same shape.
+        if (sessionCostAccumulator != null && costThreadId != null) {
+            var costSnapshot = sessionCostAccumulator.getSnapshot(costThreadId);
+            if (costSnapshot.inputTokens() > 0 || costSnapshot.llmCalls() > 0) {
+                Map<String, Object> costData = new HashMap<>();
+                costData.put("inputTokens", costSnapshot.inputTokens());
+                costData.put("outputTokens", costSnapshot.outputTokens());
+                costData.put("llmCalls", costSnapshot.llmCalls());
+                costData.put("totalCost", costSnapshot.totalCost());
+                costData.put("isolated", isolatedSnapshot);
+                result.put("costData", costData);
+                logger.info("A2A response for thread {} carries costData: in={}, out={}, calls={}, cost={}, isolated={}",
+                        costThreadId, costSnapshot.inputTokens(), costSnapshot.outputTokens(),
+                        costSnapshot.llmCalls(), costSnapshot.totalCost(), isolatedSnapshot);
+            }
+        }
+
         Map<String, Object> response = new HashMap<>();
         response.put("jsonrpc", "2.0");
         response.put("id", requestId);
diff --git a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/federation/MetaPlaybookRouting.java b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/federation/MetaPlaybookRouting.java
index e4899b5..0a929fa 100644
--- a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/federation/MetaPlaybookRouting.java
+++ b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/federation/MetaPlaybookRouting.java
@@ -27,6 +27,15 @@ import java.util.regex.Pattern;
  */
 public final class MetaPlaybookRouting {
 
+    /**
+     * taskParams marker for a deliberate meta playbook-door forward. The cell's delegated
+     * door must not demote a marked turn to ReAct at its first-turn gate just because the
+     * conversation touched the cell before; with no {@code playbookId} alongside it, the
+     * cell still routes the verbatim text with its own router - the marker moves only the
+     * gate, never the choice of playbook.
+     */
+    public static final String DETERMINISTIC_FORWARD_PARAM = "deterministicForward";
+
     public enum Kind { PASS_THROUGH, ASK_DOMAIN, DELEGATE }
 
     public record Decision(Kind kind,
@@ -70,7 +79,8 @@ public final class MetaPlaybookRouting {
         return new Decision(Kind.ASK_DOMAIN, id, name, svc, null, null, askMessage(name, svc, cells.keySet()));
     }
 
-    private static @Nullable String agentKeyOf(Map<String, FederationProperties.DomainCell> cells, String domain) {
+    /** The configured cell agent key for a domain, or null when the domain has no usable cell. */
+    public static @Nullable String agentKeyOf(Map<String, FederationProperties.DomainCell> cells, String domain) {
         FederationProperties.DomainCell cell = cells.get(domain);
         if (cell == null || cell.agentKey() == null || cell.agentKey().isBlank()) {
             return null;
diff --git a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/observation/cost/SessionCostAccumulator.java b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/observation/cost/SessionCostAccumulator.java
index 926d29e..fb3cc7a 100644
--- a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/observation/cost/SessionCostAccumulator.java
+++ b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/observation/cost/SessionCostAccumulator.java
@@ -63,6 +63,28 @@ public class SessionCostAccumulator {
         return currentThreadId.get();
     }
 
+    /**
+     * Runs {@code work} with the ThreadLocal bound to {@code threadId}, restoring whatever
+     * this thread carried before - so an enclosing binding (the A2A ingress binds a whole
+     * request) survives a nested one, and a pooled thread never keeps a stale id.
+     */
+    public static <T> T withThreadId(@Nullable String threadId, java.util.function.Supplier<T> work) {
+        if (threadId == null) {
+            return work.get();     // no identity to bind - inherit the enclosing binding, never blank it
+        }
+        String previous = currentThreadId.get();
+        currentThreadId.set(threadId);
+        try {
+            return work.get();
+        } finally {
+            if (previous != null) {
+                currentThreadId.set(previous);
+            } else {
+                currentThreadId.remove();
+            }
+        }
+    }
+
     /**
      * Record token usage for a session.
      * Skips empty usage (0/0 tokens) which occurs on every streaming chunk.
diff --git a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/DelegationExecutionService.java b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/DelegationExecutionService.java
index 6d2b45b..64130a8 100644
--- a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/DelegationExecutionService.java
+++ b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/DelegationExecutionService.java
@@ -706,6 +706,12 @@ public class DelegationExecutionService {
         // --- App-context resolution (text-based, non-LLM-dependent) ---
         Map<String, String> appContextDefaults = Map.of();
         boolean appContextIsAuthoritative = false;
+        // The CANONICAL name of the service whose mapping appContextDefaults came from.
+        // The identity guards below need it: params.get("service") alone cannot say what
+        // "the service's own name" is - the ReAct door's tool schema carries no service
+        // param at all, and a caller-supplied param may be an alias while the model's
+        // value is the canonical name.
+        String resolvedServiceName = null;
 
         if (appContextResolver != null) {
             // When the caller provides an explicit "service" param (e.g., from playbook context),
@@ -723,6 +729,7 @@ public class DelegationExecutionService {
                 }
                 appContextDefaults = appContextResolver.getAgentContext(analysis.knownServiceName(), agentKey);
                 appContextIsAuthoritative = true;
+                resolvedServiceName = analysis.knownServiceName();
                 logger.info("[{}] App-context (authoritative) for '{}': {}",
                         agentKey, analysis.knownServiceName(), appContextDefaults);
 
@@ -749,6 +756,7 @@ public class DelegationExecutionService {
                         ? investigationStateService.getActiveService(threadId) : null;
                 if (activeService != null) {
                     appContextDefaults = appContextResolver.getAgentContext(activeService, agentKey);
+                    resolvedServiceName = activeService;
                     if (!appContextDefaults.isEmpty()) {
                         logger.info("[{}] App-context (suggestion) for '{}' via activeService: {}",
                                 agentKey, activeService, appContextDefaults);
@@ -854,6 +862,38 @@ public class DelegationExecutionService {
                                 value = null;
                             }
                         }
+
+                        // The complement for a MAPPED service whose mapping is silent about THIS
+                        // identity key: neither guard above can fire (the override needs a ctxValue,
+                        // the unmapped guard needs an empty mapping), so a service mapped with only
+                        // cloudRoleName let a model-composed packageName straight through - and an
+                        // imagined identity filters real telemetry to zero rows. The mapping is
+                        // authoritative about which identity keys EXIST for a known service: drop
+                        // what it does not define. A key present but BLANK counts as undefined
+                        // (same convention as the guards above). The service's own name is the one
+                        // value that is never invented - it survives, like the unmapped case.
+                        if ("azure-monitor".equals(agentKey)
+                                && !appContextDefaults.isEmpty()
+                                && (ctxValue == null || ctxValue.isBlank())
+                                && value != null && !value.isBlank()
+                                && ("packageName".equalsIgnoreCase(paramName)
+                                    || "cloudRoleName".equalsIgnoreCase(paramName))) {
+                            // "The service's own name" must be judged against the RESOLVED
+                            // canonical identity, not only the raw request param: the ReAct
+                            // door's tool schema carries no service param at all, and a caller
+                            // may pass an alias while the model echoes the canonical name.
+                            String serviceParam = params.get("service");
+                            boolean ownName = (serviceParam != null
+                                            && value.trim().equalsIgnoreCase(serviceParam.trim()))
+                                    || (resolvedServiceName != null
+                                            && value.trim().equalsIgnoreCase(resolvedServiceName.trim()));
+                            if (!ownName) {
+                                logger.info("[audit] [{}] dropping identity param '{}'='{}' - not defined "
+                                        + "in the app-context mapping {} for this known service",
+                                        agentKey, paramName, value, appContextDefaults.keySet());
+                                value = null;
+                            }
+                        }
                         if (value == null || value.isBlank()) {
                             if (ctxValue != null && !ctxValue.isBlank()) {
                                 value = ctxValue;
diff --git a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookAgentAvailability.java b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookAgentAvailability.java
new file mode 100644
index 0000000..3e41c42
--- /dev/null
+++ b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookAgentAvailability.java
@@ -0,0 +1,51 @@
+package com.#exampleframe#.orchestrator.service;
+
+import com.#exampleframe#.orchestrator.config.AgentConfigResolver;
+import com.#exampleframe#.orchestrator.config.PlaybookProperties.PlaybookDefinition;
+import org.jspecify.annotations.Nullable;
+import org.slf4j.Logger;
+import org.slf4j.LoggerFactory;
+
+import java.util.Set;
+
+/**
+ * The ONE definition of "can this deployment run this playbook itself": every agent an
+ * UNCONDITIONAL step delegates to must exist in the resolved agent configs. Conditional
+ * steps may be skipped at runtime, so a missing agent there does not disqualify; absent
+ * or empty configs mean "cannot judge" and never filter.
+ *
+ * <p>Shared by {@link PlaybookRouter} (scored-match candidate filter) and
+ * {@link PlaybookResolver} (explicit-reference and forward decisions) - the meta's
+ * forwarding gate compares exactly this judgement from both classes, so the rule must
+ * never fork between them.
+ */
+final class PlaybookAgentAvailability {
+
+    private static final Logger logger = LoggerFactory.getLogger(PlaybookAgentAvailability.class);
+
+    private PlaybookAgentAvailability() {}
+
+    /**
+     * @param label what to call the playbook in the log line (id or display name)
+     */
+    static boolean available(@Nullable PlaybookDefinition definition,
+                             @Nullable AgentConfigResolver agentConfigResolver,
+                             String label) {
+        if (agentConfigResolver == null || definition == null || definition.steps() == null) {
+            return true;
+        }
+        Set<String> have = agentConfigResolver.getAgentConfigs().keySet();
+        if (have.isEmpty()) {
+            return true;                       // configs not resolved yet - do not filter blindly
+        }
+        for (var step : definition.steps()) {
+            boolean conditional = step.condition() != null && !step.condition().isBlank();
+            if (!conditional && step.agent() != null && !have.contains(step.agent())) {
+                logger.info("Playbook '{}' not runnable on this deployment: unconditional step '{}' needs "
+                        + "agent '{}', this deployment has {}", label, step.id(), step.agent(), have);
+                return false;
+            }
+        }
+        return true;
+    }
+}
diff --git a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookExecutor.java b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookExecutor.java
index 63fee45..baf533d 100644
--- a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookExecutor.java
+++ b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookExecutor.java
@@ -124,10 +124,12 @@ public class PlaybookExecutor {
         logger.info("[playbook] Timeout budget: steps={}s, synthesis-buffer={}s, outer={}s",
                 stepTimeout, SYNTHESIS_BUFFER_SECONDS, outerTimeout);
 
-        return Mono.fromCallable(() -> executeSteps(playbook, params, threadId, originalQuery, true, deadline))
+        return Mono.fromCallable(() -> withCostThread(threadId,
+                        () -> executeSteps(playbook, params, threadId, originalQuery, true, deadline)))
                 .subscribeOn(reactor.core.scheduler.Schedulers.boundedElastic())
                 .flatMapMany(results ->
-                        Flux.fromIterable(buildTerminalEvents(playbook, results, originalQuery)))
+                        Flux.fromIterable(withCostThread(threadId,
+                                () -> buildTerminalEvents(playbook, results, originalQuery))))
                 .timeout(Duration.ofSeconds(outerTimeout))
                 .onErrorResume(e -> {
                     logger.error("Playbook execution failed: {}", e.getMessage(), e);
@@ -155,8 +157,10 @@ public class PlaybookExecutor {
         if (threadId == null) {
             threadId = investigationStateService.resolveCurrentThreadId();
         }
+        boolean threadIdGenerated = false;
         if (threadId == null) {
             threadId = UUID.randomUUID().toString();
+            threadIdGenerated = true;
             investigationStateService.initThread(threadId);
             logger.info("Generated fallback threadId for blocking playbook execution: {}", threadId);
         }
@@ -165,7 +169,16 @@ public class PlaybookExecutor {
         int stepTimeout = Math.max(effectiveTimeout - SYNTHESIS_BUFFER_SECONDS, 60);
         Instant deadline = Instant.now().plusSeconds(stepTimeout);
 
-        List<StepResult> results = executeSteps(playbook, params, threadId, originalQuery, false, deadline);
+        // Cost identity: a MINTED thread id is one no cost panel ever reads - if the calling
+        // thread already carries a real session binding (the tool door: the controller bound
+        // the session and reactor propagated it here while resolveCurrentThreadId drew a
+        // blank), recording under the mint would LOSE that spend. Prefer the live binding;
+        // the mint is only a last resort so the accumulator is never keyed by null.
+        String liveBinding = com.#exampleframe#.orchestrator.observation.cost.SessionCostAccumulator.getCurrentThreadId();
+        final String costThread = (threadIdGenerated && liveBinding != null) ? liveBinding : threadId;
+        final String execThread = threadId;
+        List<StepResult> results = withCostThread(costThread,
+                () -> executeSteps(playbook, params, execThread, originalQuery, false, deadline));
 
         // Check for HITL - throw so caller can wrap in ToolExecutionException
         for (StepResult result : results) {
@@ -174,7 +187,18 @@ public class PlaybookExecutor {
             }
         }
 
-        return synthesizeReport(playbook, results, originalQuery);
+        return withCostThread(costThread, () -> synthesizeReport(playbook, results, originalQuery));
+    }
+
+    /**
+     * The executor owns cost attribution so EVERY door - UI gateway, delegated ingress,
+     * tool path - gets its playbook llm calls (step extraction, synthesis) attributed to
+     * the session; without it the ObservationFilter sees no threadId on the playbook path
+     * and the spend silently misses the cost panel (child-agent costs travel separately
+     * and were never affected).
+     */
+    private static <T> T withCostThread(String threadId, java.util.function.Supplier<T> work) {
+        return com.#exampleframe#.orchestrator.observation.cost.SessionCostAccumulator.withThreadId(threadId, work);
     }
 
 
diff --git a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookGateway.java b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookGateway.java
index 9840106..bb35a84 100644
--- a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookGateway.java
+++ b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookGateway.java
@@ -149,6 +149,19 @@ public class PlaybookGateway {
             String threadId,
             String userId,
             String requestNamespace) {
+        // The gateway's own llm calls (param extraction, prompt-reply parsing) run
+        // synchronously in here, BEFORE any executor - bind them to the conversation so
+        // the ObservationFilter attributes their cost; the executor and forward paths
+        // re-bind for their own async work. Restores whatever this thread carried.
+        return com.#exampleframe#.orchestrator.observation.cost.SessionCostAccumulator.withThreadId(
+                threadId, () -> tryExecuteBound(userMessage, threadId, userId, requestNamespace));
+    }
+
+    private Optional<Flux<ServerSentEvent<String>>> tryExecuteBound(
+            String userMessage,
+            String threadId,
+            String userId,
+            String requestNamespace) {
 
         // Meta forwarding continuation: an answer to "which domain should run it?" or to the
         // param prompt of a playbook the meta is about to hand to a cell. Checked before the
@@ -162,9 +175,13 @@ public class PlaybookGateway {
             if (metaPending != null) {
                 if (isMetaForwardCancellation(userMessage)) {
                     logger.info("[meta-playbook] pending '{}' cancelled by the user", metaPending.playbookId());
-                    return Optional.of(Flux.just(buildAssistantEvent(
-                            "Okay - I've dropped the pending **" + metaPending.playbookId()
-                                    + "** playbook request. What would you like to do instead?")));
+                    // Name the playbook only when the USER named it - a scored pending's id is
+                    // the meta's own guess, and quoting it back reads as "you asked for this".
+                    return Optional.of(Flux.just(buildAssistantEvent(metaPending.explicitReference()
+                            ? "Okay - I've dropped the pending **" + metaPending.playbookId()
+                                    + "** playbook request. What would you like to do instead?"
+                            : "Okay - I've dropped that pending investigation. "
+                                    + "What would you like to do instead?")));
                 }
                 if (isMetaForwardPivot(userMessage, metaPending)) {
                     logger.info("[meta-playbook] pending '{}' dropped - the new message is its own request",
@@ -181,6 +198,12 @@ public class PlaybookGateway {
         PlaybookMatch resolvedMatch;
         Map<String, String> params;
         String originalQuery;  // the ORIGINAL problem statement, not the follow-up
+        // True only when the USER named the playbook ("run the service-slow playbook"). A
+        // forwarded scored match must NOT pin the meta's choice on the owning cell - the cell's
+        // own router is the authority on how to investigate its domain, and the meta's ranking
+        // (unfiltered, possibly a different playbook set) has been seen to pick oracle-slow
+        // where the cell picks service-slow. The meta only delegates.
+        boolean explicitReference = false;
 
         if (pending != null) {
             logger.info("Resuming pending playbook '{}' - merging user response into params", pending.playbookId());
@@ -220,9 +243,13 @@ public class PlaybookGateway {
                     : resolver.resolveByExplicitId(userMessage)).orElse(null);
             if (explicit != null) {
                 resolvedMatch = new PlaybookMatch(explicit.getKey(), explicit.getValue(), false);
+                explicitReference = true;
             } else {
-                // Step 3: Try fresh playbook match
-                Optional<PlaybookMatch> match = router.match(userMessage);
+                // Step 3: Try fresh playbook match. A meta matches WITHOUT the agents-available
+                // filter: it cannot run these playbooks itself, but recognising the question as
+                // playbook-shaped is what routes it to the owning cell deterministically instead
+                // of the ReAct path (paraphrased prompt, HITL, divergent answers).
+                Optional<PlaybookMatch> match = router.match(userMessage, !metaForwardingEnabled());
                 if (match.isEmpty()) return Optional.empty();
                 resolvedMatch = match.get();
             }
@@ -234,11 +261,19 @@ public class PlaybookGateway {
                     playbookProperties.paramDefinitions());
         }
 
+        // A playbook this deployment cannot run itself belongs to the owning domain cell -
+        // decided ONCE, here, because it also disables the soft-match rejection below: on the
+        // meta, deferring to ReAct means a lossy LLM-delegated turn, while forwarding hands
+        // the verbatim question to the cell, whose own gateway applies the very same
+        // soft-match judgement the direct door would.
+        boolean forwardToCell = metaForwardingEnabled()
+                && !resolver.agentsAvailable(resolvedMatch.definition());
+
         // Soft-match gate: if the match was soft (low confidence) AND the user's query
         // already provided all required params, reject the match - the query is specific
         // enough that ReAct can handle it properly. Soft matches are only useful when
         // the query is vague and the user needs to be prompted for missing details.
-        if (resolvedMatch.softMatch()) {
+        if (resolvedMatch.softMatch() && !forwardToCell) {
             List<MissingParam> preCheckMissing = resolver.findMissingParams(
                     resolvedMatch.definition().params(), params, playbookProperties.paramDefinitions());
             if (preCheckMissing.isEmpty()) {
@@ -250,8 +285,6 @@ public class PlaybookGateway {
                     resolvedMatch.playbookId(), preCheckMissing.size());
         }
 
-        logger.info("Playbook matched: {} - bypassing ReAct", resolvedMatch.playbookId());
-
         // Inject request-level namespace if explicitly provided by the API caller.
         // LLM extraction takes priority (it already ran above), but if the LLM didn't
         // extract a namespace AND the API request provided one, use the request value.
@@ -280,12 +313,25 @@ public class PlaybookGateway {
         resolver.applyAppContext(params, originalQuery);
 
         // Meta: a playbook this deployment cannot run itself belongs to the owning domain cell.
-        if (metaForwardingEnabled() && !resolver.agentsAvailable(resolvedMatch.definition())) {
+        if (forwardToCell) {
+            // A scored match is the meta's own guess, so it may capture the turn only when
+            // the request itself pins a service the mapping owns - the ask-domain and param
+            // prompts speak of "the playbook you asked for", which is only true for explicit
+            // references. Anything vaguer keeps the pre-forwarding behavior: ReAct.
+            if (!explicitReference && resolveOwnedService(originalQuery) == null) {
+                logger.info("[meta-playbook] scored match '{}' pins no mapped service - leaving the turn to ReAct",
+                        resolvedMatch.playbookId());
+                return Optional.empty();
+            }
             return Optional.of(forwardToOwningCell(
-                    resolvedMatch, params, null, callerProvidedKeys,
+                    resolvedMatch, params, null, callerProvidedKeys, explicitReference,
                     originalQuery, threadId, userId));
         }
 
+        // Only now is the turn certainly running HERE - logged after the forward/gate
+        // decisions so "bypassing ReAct" is never followed by "leaving the turn to ReAct".
+        logger.info("Playbook matched: {} - bypassing ReAct", resolvedMatch.playbookId());
+
         // Check for missing params that have prompts defined - ask user instead of running blind
         List<MissingParam> missingParams = resolver.findMissingParams(
                 resolvedMatch.definition().params(), params, playbookProperties.paramDefinitions());
@@ -388,19 +434,23 @@ public class PlaybookGateway {
     }
 
     /**
-     * META door for an explicit playbook request. The meta has no infrastructure agents, so it
+     * META door for a playbook-shaped request. The meta has no infrastructure agents, so it
      * never runs the playbook itself: it names the owning cell from the service mapping (or the
      * user's own domain choice from a previous ask) and hands the user's words over verbatim -
-     * the cell's delegated door resolves the same reference and runs that playbook - or asks
-     * which domain when the request pins none. Only params the user alone can supply (timeRange,
-     * the service itself) are collected here; infrastructure scope (namespace, schema, ...) is
-     * the owning cell's to fill from ITS mapping - the meta's may hold only domain ownership.
+     * or asks which domain when the request pins none. Only params the user alone can supply
+     * (timeRange, the service itself) are collected here; infrastructure scope (namespace,
+     * schema, ...) is the owning cell's to fill from ITS mapping - the meta's may hold only
+     * domain ownership. {@code explicitReference} distinguishes the user NAMING a playbook
+     * (forwarded as a pinned {@code taskParams.playbookId} the cell resolves by id) from the
+     * meta's own scored guess (not forwarded at all - the cell's router re-routes the verbatim
+     * text exactly as its direct door would, so both doors converge).
      */
     private Flux<ServerSentEvent<String>> forwardToOwningCell(
             PlaybookMatch match,
             Map<String, String> params,
             @Nullable String chosenDomain,
             Set<String> callerProvidedKeys,
+            boolean explicitReference,
             String originalQuery,
             String threadId,
             String userId) {
@@ -449,7 +499,7 @@ public class PlaybookGateway {
                     match.playbookId(), service);
             pendingMetaForwards.put(threadId, new PendingMetaForward(
                     match.playbookId(), match.definition(), params, null, callerProvidedKeys,
-                    List.of(AWAITING_DOMAIN), originalQuery, Instant.now()));
+                    explicitReference, List.of(AWAITING_DOMAIN), originalQuery, Instant.now()));
             return Flux.just(buildAssistantEvent(decision.message()));
         }
 
@@ -459,6 +509,9 @@ public class PlaybookGateway {
 
         // Collect only what the user alone can supply - timeRange, and the service itself after
         // a bare domain choice. Infrastructure params stay unfilled for the cell to resolve.
+        // Soft matches prompt too: the cell's DIRECT door prompts for a soft match's missing
+        // params and then runs it, so the meta mirrors that - and the meta cannot know
+        // whether the cell's own ranking will judge the same text soft or strong.
         List<MissingParam> userOnly = resolver.findMissingParams(
                         match.definition().params(), params, playbookProperties.paramDefinitions())
                 .stream()
@@ -471,7 +524,8 @@ public class PlaybookGateway {
                     userOnly.stream().map(MissingParam::key).toList());
             pendingMetaForwards.put(threadId, new PendingMetaForward(
                     match.playbookId(), match.definition(), params, decision.domain(), callerProvidedKeys,
-                    userOnly.stream().map(MissingParam::key).toList(), originalQuery, Instant.now()));
+                    explicitReference, userOnly.stream().map(MissingParam::key).toList(),
+                    originalQuery, Instant.now()));
             return Flux.just(buildParamPromptEvent(
                     match.playbookId(), match.definition().name(), userOnly));
         }
@@ -483,23 +537,39 @@ public class PlaybookGateway {
         // its namespaces and schemas. Values the user or the API caller stated themselves
         // (callerProvidedKeys, including reply-volunteered ones) do travel: user provenance
         // outranks either mapping.
+        // Control keys FIRST: the delegation layer truncates taskParams to a fixed entry
+        // budget in insertion order, and losing the pin or the marker silently demotes the
+        // cell to ReAct - a dropped ordinary param only loses one prefill.
+        // Only a playbook the USER named travels as a pinned id (the cell's resolveById honors
+        // it, even on follow-up turns). A scored match is the meta's own guess: the cell gets
+        // the verbatim question and lets ITS router pick - the same routing the direct door
+        // runs - so both doors converge on the same playbook (or the same ReAct fallback).
+        // The marker keeps the cell's FIRST-TURN gate from demoting a later-turn forward to
+        // ReAct: without it, every scored forward after the conversation's first cell contact
+        // would silently lose the deterministic path this door exists for.
         final Map<String, String> extra = new LinkedHashMap<>();
+        if (explicitReference) {
+            extra.put("playbookId", match.playbookId());
+        }
+        extra.put(MetaPlaybookRouting.DETERMINISTIC_FORWARD_PARAM, "true");
+        extra.put("domain", decision.domain());
         params.forEach((k, v) -> {
             if (v != null && !v.isBlank() && !"UNKNOWN".equalsIgnoreCase(v)
+                    && !extra.containsKey(k)   // a param may never displace a control key
                     && (!PlaybookResolver.APP_CONTEXT_PARAM_KEYS.contains(k) || callerProvidedKeys.contains(k))) {
                 extra.put(k, v);
             }
         });
-        extra.put("playbookId", match.playbookId());
-        extra.put("domain", decision.domain());
 
         // Owner of any HITL the cell raises: captured HERE, at the HTTP boundary, because the
         // delegation runs on a worker thread whose security context is empty - stored under
         // "anonymous" the real user's later approval would be refused as cross-user access.
         final String requestOwner = com.#exampleframe#.orchestrator.security.CallerContext.capture().name();
 
-        logger.info("[meta-playbook] '{}' -> {} cell '{}' (service='{}') - forwarding the request verbatim ({} chars)",
-                match.playbookId(), decision.domain(), cellKey, decision.service(), originalQuery.length());
+        logger.info("[meta-playbook] '{}' -> {} cell '{}' (service='{}') - forwarding the request verbatim "
+                        + "({} chars, {})",
+                match.playbookId(), decision.domain(), cellKey, decision.service(), originalQuery.length(),
+                explicitReference ? "pinned by the user's reference" : "cell routes it");
 
         investigationStateService.recordUserMessage(threadId, originalQuery);
 
@@ -509,7 +579,7 @@ public class PlaybookGateway {
             // delegation instead of the request silently dying to context-free ReAct.
             pendingMetaForwards.put(threadId, new PendingMetaForward(
                     match.playbookId(), match.definition(), params, decision.domain(), callerProvidedKeys,
-                    List.of(), originalQuery, Instant.now()));
+                    explicitReference, List.of(), originalQuery, Instant.now()));
             return Flux.just(ServerSentEvent.<String>builder()
                     .event("error")
                     .data("Another request is already streaming for this conversation. "
@@ -617,6 +687,7 @@ public class PlaybookGateway {
             Map<String, String> params,
             @Nullable String chosenDomain,
             Set<String> callerProvidedKeys,
+            boolean explicitReference,
             List<String> awaiting,
             String originalUserMessage,
             Instant createdAt
@@ -743,7 +814,31 @@ public class PlaybookGateway {
 
         PlaybookMatch match = new PlaybookMatch(pending.playbookId(), pending.definition(), false);
         return forwardToOwningCell(match, params, chosenDomain, callerKeys,
-                pending.originalUserMessage(), threadId, userId);
+                pending.explicitReference(), pending.originalUserMessage(), threadId, userId);
+    }
+
+    /**
+     * The service the user's OWN TEXT pins - never the LLM-extracted value, whose
+     * hallucinations must not route a turn (the provenance rule the explicit flow already
+     * enforces) - and only when a domain owns it AND that domain has a usable cell (the
+     * same predicate {@link MetaPlaybookRouting#decide} applies, so a passing gate can
+     * never land in ASK_DOMAIN's "You asked me to run X" wording for a playbook the user
+     * never named). Null means "nothing routable": the scored match stays with ReAct.
+     */
+    private @Nullable String resolveOwnedService(String originalQuery) {
+        if (appContextResolver == null || domainCellRouter == null) {
+            return null;
+        }
+        String service = appContextResolver.analyzeText(originalQuery).knownServiceName();
+        if (service == null) {
+            return null;
+        }
+        String domain = domainCellRouter.owningDomain(service).orElse(null);
+        if (domain == null) {
+            return null;
+        }
+        return MetaPlaybookRouting.agentKeyOf(federationProperties.domainsOrEmpty(), domain) != null
+                ? service : null;
     }
 
     /** The configured delegation tool name for a cell (what the UI labels the panel with). */
diff --git a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookResolver.java b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookResolver.java
index e67a5e2..ac55a61 100644
--- a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookResolver.java
+++ b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookResolver.java
@@ -212,22 +212,8 @@ public class PlaybookResolver {
      * "cannot judge": no filtering.
      */
     public boolean agentsAvailable(@Nullable PlaybookDefinition definition) {
-        if (agentConfigResolver == null || definition == null || definition.steps() == null) {
-            return true;
-        }
-        Set<String> have = agentConfigResolver.getAgentConfigs().keySet();
-        if (have.isEmpty()) {
-            return true;
-        }
-        for (var step : definition.steps()) {
-            boolean conditional = step.condition() != null && !step.condition().isBlank();
-            if (!conditional && step.agent() != null && !have.contains(step.agent())) {
-                logger.info("Playbook '{}' not resolvable by explicit reference here: step '{}' needs "
-                        + "agent '{}', this deployment has {}", definition.name(), step.id(), step.agent(), have);
-                return false;
-            }
-        }
-        return true;
+        return PlaybookAgentAvailability.available(definition, agentConfigResolver,
+                definition != null ? definition.name() : "<none>");
     }
 
     /**
diff --git a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookRouter.java b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookRouter.java
index 18a308c..bfa895f 100644
--- a/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookRouter.java
+++ b/#exampleframe#-orchestrator/src/main/java/com/#exampleframe#/orchestrator/service/PlaybookRouter.java
@@ -210,33 +210,31 @@ public class PlaybookRouter {
      */
     /** True when every agent the playbook's steps delegate to exists in this deployment. */
     private boolean agentsAvailable(PlaybookCandidate candidate) {
-        if (agentConfigResolver == null || candidate.definition() == null
-                || candidate.definition().steps() == null) {
-            return true;
-        }
-        java.util.Set<String> have = agentConfigResolver.getAgentConfigs().keySet();
-        if (have.isEmpty()) {
-            return true;                       // configs not resolved yet - do not filter blindly
-        }
-        for (var step : candidate.definition().steps()) {
-            boolean conditional = step.condition() != null && !step.condition().isBlank();
-            if (!conditional && step.agent() != null && !have.contains(step.agent())) {
-                logger.info("Playbook '{}' not applicable here: unconditional step '{}' needs agent "
-                        + "'{}', this deployment has {}", candidate.playbookId(), step.id(), step.agent(), have);
-                return false;
-            }
-        }
-        return true;
+        return PlaybookAgentAvailability.available(
+                candidate.definition(), agentConfigResolver, candidate.playbookId());
     }
 
     public Optional<PlaybookMatch> match(String userQuery) {
+        return match(userQuery, true);
+    }
+
+    /**
+     * @param requireAgentsAvailable when true (every non-meta caller), candidates whose steps
+     *        need an agent this deployment lacks are dropped - running one would delegate into
+     *        thin air. A meta that FORWARDS matches passes false: it never runs the playbook
+     *        itself, it only needs to recognise the question as playbook-shaped so the gateway
+     *        can hand it to the owning cell instead of a lossy ReAct paraphrase.
+     */
+    public Optional<PlaybookMatch> match(String userQuery, boolean requireAgentsAvailable) {
         List<PlaybookCandidate> candidates = activeMatcher.match(
                 userQuery, routingProperties.topK());
 
         // A playbook drives agents directly, bypassing the ReAct loop: one whose steps need an
         // agent this deployment does not have (a meta routes to cells; a cell may run only some
         // agent types) would delegate into thin air, so it is not a candidate here.
-        candidates = candidates.stream().filter(this::agentsAvailable).toList();
+        if (requireAgentsAvailable) {
+            candidates = candidates.stream().filter(this::agentsAvailable).toList();
+        }
 
         if (candidates.isEmpty()) {
             noMatchCounter.increment();
diff --git a/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/api/controller/A2aIngressHitlTest.java b/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/api/controller/A2aIngressHitlTest.java
index 92473d2..36ce7d9 100644
--- a/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/api/controller/A2aIngressHitlTest.java
+++ b/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/api/controller/A2aIngressHitlTest.java
@@ -49,7 +49,7 @@ class A2aIngressHitlTest {
     void setUp() {
         state = new InvestigationStateService(agentConfigResolver, null);
         ingress = new A2aIngressController(runner, new OrchestratorProperties(), hitlClient, state, invocationService,
-                null /* playbookResolver */, null /* playbookExecutor */, null /* checkpointSaver */, null /* threadStore */);
+                null /* playbookResolver */, null /* playbookExecutor */, null /* checkpointSaver */, null /* threadStore */, null /* costAccumulator */);
     }
 
     @SuppressWarnings("unchecked")
diff --git a/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/api/controller/A2aIngressPlaybookTest.java b/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/api/controller/A2aIngressPlaybookTest.java
index 55548a5..bf241b8 100644
--- a/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/api/controller/A2aIngressPlaybookTest.java
+++ b/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/api/controller/A2aIngressPlaybookTest.java
@@ -57,7 +57,7 @@ class A2aIngressPlaybookTest {
     void setUp() {
         state = new InvestigationStateService(agentConfigResolver, null);
         ingress = new A2aIngressController(runner, new OrchestratorProperties(), null, state, null,
-                resolver, executor, checkpointSaver, threadStore);
+                resolver, executor, checkpointSaver, threadStore, null /* costAccumulator */);
     }
 
     private static Map<String, Object> message(String text, Map<String, Object> extraParams) {
@@ -157,6 +157,32 @@ class A2aIngressPlaybookTest {
         verify(resolver, never()).resolve(anyString(), anyMap());
     }
 
+    @Test
+    void followUpTurnWithTheDeterministicForwardMarkerRunsTheCellsOwnRouting() throws Exception {
+        // A meta's SCORED forward pins no playbookId - the cell routes the text itself -
+        // but it does mark the turn as a deliberate forward. The first-turn gate must
+        // honor the marker, or every scored forward after turn 1 silently becomes ReAct.
+        state.initThread("a2a-meta-thread-1");   // prior turn exists
+        Map<String, String> params = Map.of("service", "claims-service", "timeRange", "2h");
+        when(resolver.resolve(eq("why is claims-service slow over the last 2h"), anyMap()))
+                .thenReturn(Optional.of(
+                        new PlaybookResolver.Resolution("service-slow", DEFINITION, false, params, List.of())));
+        when(executor.executeBlocking(eq(DEFINITION), eq(params), anyString(), anyString()))
+                .thenReturn("# RCA report");
+
+        Map<String, Object> done = result(ingress.handleMessage(
+                message("why is claims-service slow over the last 2h",
+                        Map.of("contextId", "meta-thread-1",
+                                "rawTask", "why is claims-service slow over the last 2h",
+                                "taskParams", Map.of("deterministicForward", "true",
+                                        "service", "claims-service", "timeRange", "2h")))).getBody());
+
+        assertThat(done.get("status")).isEqualTo("completed");
+        assertThat(done.get("output")).isEqualTo("# RCA report");
+        verifyNoInteractions(runner);   // playbook path, not ReAct
+        verify(resolver).resolve(anyString(), anyMap());   // the CELL's router picked, no pin
+    }
+
     @Test
     void forwardedPlaybookIdSelectsThePlaybookEvenWhenTheTextNamesAnotherOne() throws Exception {
         // Config drift or overlapping references: the ID pins the selection; text matching
diff --git a/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/service/AppContextDelegationTest.java b/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/service/AppContextDelegationTest.java
index 32cb62f..ca8658a 100644
--- a/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/service/AppContextDelegationTest.java
+++ b/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/service/AppContextDelegationTest.java
@@ -63,6 +63,13 @@ class AppContextDelegationTest {
         ledger.put("oracle.tables", "INVOICES,POSTINGS");
         services.put("ledger-service", ledger);
 
+        // Telemetry identity by cloudRoleName ONLY - the mapping deliberately defines no
+        // packageName. The guard tests below depend on that silence.
+        Map<String, String> portal = new LinkedHashMap<>();
+        portal.put("aliases", "portal,portal-api,PortalService");
+        portal.put("azure-monitor.cloudRoleName", "portal-app");
+        services.put("portal-service", portal);
+
         appContextResolver = new AppContextResolver(
                 new AppContextProperties(services), new StandardEnvironment(), null);
 
@@ -620,6 +627,126 @@ class AppContextDelegationTest {
 
             assertThat(input).contains("[Namespace: production]");
         }
+
+        @Test
+        void aMappedServicesUndefinedIdentityKeyDropsTheModelsValue() {
+            // THE GAP between the two guards above: portal-service IS mapped (cloudRoleName only),
+            // so the unmapped-guard cannot fire; packageName has no ctxValue, so the override
+            // cannot fire either. Without the silent-key guard the model's composed packageName
+            // sailed through and filtered real telemetry to zero rows.
+            configureAgent("azure-monitor",
+                    orderedMap("task", "...", "packageName", "Java package prefix",
+                            "cloudRoleName", "App Insights cloud role"),
+                    Map.of());
+
+            String input = delegationService.buildAgentInput(
+                    "azure-monitor", "portal-service is slow", THREAD_ID,
+                    Map.of("packageName", "com.imagined.portal"), null);
+
+            assertThat(input).contains("[CloudRoleName: portal-app]");     // mapping identity kept
+            assertThat(input).doesNotContain("com.imagined.portal");       // invented key dropped
+        }
+
+        @Test
+        void theSymmetricCaseDropsAnInventedCloudRoleName() {
+            // checkout-service maps packageName only; a model-supplied cloudRoleName is not defined
+            // there and must be dropped, while the mapped packageName stays authoritative.
+            configureAgent("azure-monitor",
+                    orderedMap("task", "...", "packageName", "Java package prefix",
+                            "cloudRoleName", "App Insights cloud role"),
+                    Map.of());
+
+            String input = delegationService.buildAgentInput(
+                    "azure-monitor", "checkout-service is slow", THREAD_ID,
+                    Map.of("cloudRoleName", "InventedRole"), null);
+
+            assertThat(input).contains("[PackageName: checkout-api]");
+            assertThat(input).doesNotContain("InventedRole");
+        }
+
+        @Test
+        void aMappedCloudRoleNameOverridesTheModelsDifferentValue() {
+            // cloudRoleName parity with the packageName tests above: when the mapping DOES define
+            // it, the mapping wins over the model's guess (authoritative mode).
+            configureAgent("azure-monitor",
+                    orderedMap("task", "...", "cloudRoleName", "App Insights cloud role"),
+                    Map.of());
+
+            String input = delegationService.buildAgentInput(
+                    "azure-monitor", "portal-service is slow", THREAD_ID,
+                    Map.of("cloudRoleName", "wrong-role"), null);
+
+            assertThat(input).contains("[CloudRoleName: portal-app]");
+            assertThat(input).doesNotContain("wrong-role");
+        }
+
+        @Test
+        void theSilentKeyGuardAlsoFiresOnAFollowUpHop() {
+            // Suggestion mode (activeService, no service named in the task): the mapping is still
+            // authoritative about which identity keys exist for the active subject.
+            configureAgent("azure-monitor",
+                    orderedMap("task", "...", "packageName", "Java package prefix",
+                            "cloudRoleName", "App Insights cloud role"),
+                    Map.of());
+            when(investigationStateService.getActiveService(THREAD_ID)).thenReturn("portal-service");
+
+            String input = delegationService.buildAgentInput(
+                    "azure-monitor", "check exceptions for the slow pod", THREAD_ID,
+                    Map.of("packageName", "java-healthy"), null);
+
+            assertThat(input).contains("[CloudRoleName: portal-app]");
+            assertThat(input).doesNotContain("java-healthy");
+        }
+
+        @Test
+        void theServicesOwnNameSurvivesTheSilentKeyGuard() {
+            // Same exception the unmapped guard makes: a value equal to the service's own name is
+            // never "invented" - it stays, keeping the two guards' philosophy identical.
+            configureAgent("azure-monitor",
+                    orderedMap("task", "...", "packageName", "Java package prefix",
+                            "cloudRoleName", "App Insights cloud role"),
+                    Map.of());
+
+            String input = delegationService.buildAgentInput(
+                    "azure-monitor", "portal-service is slow", THREAD_ID,
+                    Map.of("service", "portal-service", "packageName", "portal-service"), null);
+
+            assertThat(input).contains("[PackageName: portal-service]");
+        }
+
+        @Test
+        void theOwnNameExceptionWorksWithoutAServiceParam() {
+            // THE REAL ReAct-door shape: the azure tool schema has no "service" key at all, so
+            // the exception must be judged against the RESOLVED identity from the task text -
+            // an adversarial review caught the raw-param-only version dropping the service's
+            // own name on exactly this door.
+            configureAgent("azure-monitor",
+                    orderedMap("task", "...", "packageName", "Java package prefix",
+                            "cloudRoleName", "App Insights cloud role"),
+                    Map.of());
+
+            String input = delegationService.buildAgentInput(
+                    "azure-monitor", "portal-service is slow", THREAD_ID,
+                    Map.of("packageName", "portal-service"), null);
+
+            assertThat(input).contains("[PackageName: portal-service]");
+        }
+
+        @Test
+        void theOwnNameExceptionRecognizesTheCanonicalNameBehindAnAlias() {
+            // Caller routes by alias ("portal"), the model echoes the canonical name - the
+            // resolved identity, not string-equality with the raw param, decides "own name".
+            configureAgent("azure-monitor",
+                    orderedMap("task", "...", "packageName", "Java package prefix",
+                            "cloudRoleName", "App Insights cloud role"),
+                    Map.of());
+
+            String input = delegationService.buildAgentInput(
+                    "azure-monitor", "the portal is slow", THREAD_ID,
+                    Map.of("service", "portal", "packageName", "portal-service"), null);
+
+            assertThat(input).contains("[PackageName: portal-service]");
+        }
     }
 
     // ---- Utility ----
diff --git a/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/service/PlaybookGatewayMetaForwardingTest.java b/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/service/PlaybookGatewayMetaForwardingTest.java
index 311a90d..e9b44fb 100644
--- a/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/service/PlaybookGatewayMetaForwardingTest.java
+++ b/#exampleframe#-orchestrator/src/test/java/com/#exampleframe#/orchestrator/service/PlaybookGatewayMetaForwardingTest.java
@@ -35,6 +35,7 @@ import java.util.Optional;
 
 import static org.assertj.core.api.Assertions.assertThat;
 import static org.mockito.ArgumentMatchers.any;
+import static org.mockito.ArgumentMatchers.anyBoolean;
 import static org.mockito.ArgumentMatchers.anyMap;
 import static org.mockito.ArgumentMatchers.anyString;
 import static org.mockito.ArgumentMatchers.argThat;
@@ -332,12 +333,125 @@ class PlaybookGatewayMetaForwardingTest {
 
     @Test
     void noExplicitReferencePassesThroughToReact() {
-        when(router.match(anyString())).thenReturn(Optional.empty());
+        when(router.match(anyString(), anyBoolean())).thenReturn(Optional.empty());
 
         assertThat(gateway.tryExecute("why is payments-api slow", "t-pass", "user-1", null)).isEmpty();
         verify(delegationExecutionService, never()).delegateToAgentPinned(anyString(), anyString(), anyString(), anyMap(), any());
     }
 
+    @Test
+    void scoredMatchAtTheMetaForwardsVerbatimWithoutPinningThePlaybook() {
+        // "why is payments-api slow" is nobody's explicit reference - the meta's scored match
+        // only recognises the question as playbook-shaped. The cell must receive the verbatim
+        // text with NO playbookId: the meta's own ranking (unfiltered, and seen picking
+        // oracle-slow where the cell picks service-slow) must not choose for the cell.
+        String q = "why is payments-api slow over the last 24h";
+        when(router.match(eq(q), eq(false)))
+                .thenReturn(Optional.of(new PlaybookRouter.PlaybookMatch("service-slow", SERVICE_SLOW, false)));
+        when(router.extractParameters(anyString(), any(), any())).thenReturn(new HashMap<>(
+                Map.of("service", "payments-api", "timeRange", "24h")));
+        when(appContextResolver.analyzeText(eq(q)))
+                .thenReturn(new AppContextResolver.TextAnalysis("payments-api", false, false));
+        when(domainCellRouter.owningDomain("payments-api")).thenReturn(Optional.of("payments"));
+        when(delegationExecutionService.delegateToAgentPinned(anyString(), anyString(), anyString(), anyMap(), isNull()))
+                .thenReturn("CELL REPORT");
+
+        String out = run(q, "t-scored");
+
+        assertThat(out).contains("CELL REPORT");
+        verify(router).match(eq(q), eq(false));   // the meta matches with the agents filter OFF
+        verify(delegationExecutionService).delegateToAgentPinned(eq("payments-cell"), eq(q), eq("t-scored"),
+                argThat(m -> !m.containsKey("playbookId")
+                        && "true".equals(m.get("deterministicForward"))
+                        && "payments-api".equals(m.get("service"))
+                        && "payments".equals(m.get("domain"))), isNull());
+    }
+
+    @Test
+    void scoredMatchWithoutAMappedServiceStaysWithReact() {
+        // The meta's own guess may not interrogate the user ("You asked me to run X" -
+        // they didn't) - a scored match with no routable service falls through to ReAct,
+        // exactly the pre-forwarding behavior.
+        String q = "why is ledger-sync-svc slow over the last 24h";
+        when(router.match(eq(q), eq(false)))
+                .thenReturn(Optional.of(new PlaybookRouter.PlaybookMatch("service-slow", SERVICE_SLOW, false)));
+        when(router.extractParameters(anyString(), any(), any())).thenReturn(new HashMap<>(
+                Map.of("service", "ledger-sync-svc", "timeRange", "24h")));
+        // analyzeText knows no such service (default stub returns null), and no mapping owns it
+
+        assertThat(gateway.tryExecute(q, "t-unroutable", "user-1", null)).isEmpty();
+        verify(delegationExecutionService, never()).delegateToAgentPinned(anyString(), anyString(), anyString(), anyMap(), any());
+    }
+
+    @Test
+    void softScoredMatchWithMissingParamsPromptsLikeTheDirectDoorThenForwardsUnpinned() {
+        // The cell's DIRECT door prompts for a soft match's missing params and then runs it -
+        // the meta mirrors that, and the forward still carries no pinned playbookId.
+        String q = "payments-api feels sluggish";
+        when(router.match(eq(q), eq(false)))
+                .thenReturn(Optional.of(new PlaybookRouter.PlaybookMatch("service-slow", SERVICE_SLOW, true)));
+        when(router.extractParameters(anyString(), any(), any())).thenReturn(new HashMap<>(
+                Map.of("service", "payments-api")));
+        when(appContextResolver.analyzeText(eq(q)))
+                .thenReturn(new AppContextResolver.TextAnalysis("payments-api", false, false));
+        when(domainCellRouter.owningDomain("payments-api")).thenReturn(Optional.of("payments"));
+
+        String prompt = run(q, "t-soft-prompt");
+        assertThat(prompt).contains("timeRange");
+        verify(delegationExecutionService, never()).delegateToAgentPinned(anyString(), anyString(), anyString(), anyMap(), any());
+
+        llmExtracts("timeRange=24h");
+        when(delegationExecutionService.delegateToAgentPinned(anyString(), anyString(), anyString(), anyMap(), isNull()))
+                .thenReturn("CELL REPORT");
+        String out = run("last 24 hours", "t-soft-prompt");
+
+        assertThat(out).contains("CELL REPORT");
+        verify(delegationExecutionService).delegateToAgentPinned(eq("payments-cell"), eq(q), eq("t-soft-prompt"),
+                argThat(m -> !m.containsKey("playbookId")
+                        && "true".equals(m.get("deterministicForward"))
+                        && "24h".equals(m.get("timeRange"))), isNull());
+    }
+
+    @Test
+    void scoredMatchDoesNotTrustAnExtractedServiceForRouting() {
+        // The LLM extraction may hallucinate a service; only the TEXT may route a scored
+        // match. Text pins nothing here, so the turn stays with ReAct even though the
+        // extracted value maps cleanly.
+        String q = "the nightly batch job has been crawling since midnight";
+        when(router.match(eq(q), eq(false)))
+                .thenReturn(Optional.of(new PlaybookRouter.PlaybookMatch("service-slow", SERVICE_SLOW, false)));
+        when(router.extractParameters(anyString(), any(), any())).thenReturn(new HashMap<>(
+                Map.of("service", "payments-api", "timeRange", "since midnight")));
+        when(domainCellRouter.owningDomain("payments-api")).thenReturn(Optional.of("payments"));
+        // analyzeText default stub: the text names no known service
+
+        assertThat(gateway.tryExecute(q, "t-halluc-route", "user-1", null)).isEmpty();
+        verify(delegationExecutionService, never()).delegateToAgentPinned(anyString(), anyString(), anyString(), anyMap(), any());
+    }
+
+    @Test
+    void softScoredMatchAtTheMetaStillForwardsInsteadOfDeferringToReact() {
+        // On a cell, a soft match with every required param present defers to ReAct. On the
+        // meta, "deferring to ReAct" means a lossy LLM-delegated turn - so a forwardable soft
+        // match forwards, and the CELL applies its own soft-match judgement to the same text.
+        String q = "payments-api seems sluggish since 9am";
+        when(router.match(eq(q), eq(false)))
+                .thenReturn(Optional.of(new PlaybookRouter.PlaybookMatch("service-slow", SERVICE_SLOW, true)));
+        when(router.extractParameters(anyString(), any(), any())).thenReturn(new HashMap<>(
+                Map.of("service", "payments-api", "timeRange", "since 9am")));
+        when(appContextResolver.analyzeText(eq(q)))
+                .thenReturn(new AppContextResolver.TextAnalysis("payments-api", false, false));
+        when(domainCellRouter.owningDomain("payments-api")).thenReturn(Optional.of("payments"));
+        when(delegationExecutionService.delegateToAgentPinned(anyString(), anyString(), anyString(), anyMap(), isNull()))
+                .thenReturn("CELL REPORT");
+
+        String out = run(q, "t-soft");
+
+        assertThat(out).contains("CELL REPORT");
+        verify(delegationExecutionService).delegateToAgentPinned(eq("payments-cell"), eq(q), eq("t-soft"),
+                argThat(m -> !m.containsKey("playbookId")), isNull());
+    }
+
     @Test
     void hallucinatedExtractionCannotOverrideTheTextNamedService() {
         // Extraction claims claims-api; the user's own words name payments-api. The text wins,
@@ -434,7 +548,7 @@ class PlaybookGatewayMetaForwardingTest {
         // A topic pivot - even one naming a known service - is its own request.
         when(appContextResolver.analyzeText(anyString()))
                 .thenReturn(new AppContextResolver.TextAnalysis("checkout-service", false, false));
-        when(router.match(anyString())).thenReturn(Optional.empty());
+        when(router.match(anyString(), anyBoolean())).thenReturn(Optional.empty());
         Optional<Flux<ServerSentEvent<String>>> result = gateway.tryExecute(
                 "forget the run - why is checkout-service suddenly throwing 500s today?", "t-pivot", "user-1", null);
 
@@ -452,7 +566,7 @@ class PlaybookGatewayMetaForwardingTest {
         assertThat(ack).contains("dropped the pending");
 
         // Pending is gone: the next message routes normally.
-        when(router.match(anyString())).thenReturn(Optional.empty());
+        when(router.match(anyString(), anyBoolean())).thenReturn(Optional.empty());
         assertThat(gateway.tryExecute("hello there", "t-cancel", "user-1", null)).isEmpty();
         verify(delegationExecutionService, never()).delegateToAgentPinned(anyString(), anyString(), anyString(), anyMap(), any());
     }
